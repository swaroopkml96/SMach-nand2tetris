CHIP HalfAdder {
    IN a, b;    // 1-bit inputs
    OUT sum,    // Right bit of a + b 
        carry;  // Left bit of a + b

    PARTS:

	//Sum of a and b is Xor(a,b). Carry is And(a,b)
	And(a=a,b=b,out=carry);
	Xor(a=a,b=b,out=sum);

}
